// 2018-02-15 10:57:07
#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main() {
  int n,input;
  cin>>n;
  vector<int> V[n];
  while(n--) {
    cin>>input;
    V.push_back(input);
  }
  for(int i = 0; i < V.size(); i++)
    cout<<V[i]<<" ";
  return 0;
}
