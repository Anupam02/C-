// 2018-02-18 20:45:29
#include <iostream>
#include <algorithm>
int A[7];
int main() {
  int t,n;
  cin>>t;
  while(t--) {
    cin>>n;
    for(int i = 0; i < n; i++)
      cin>>A[i];
    next_permutation(A, A+n);
    for(int i = 0; i < n; i++)
      cout<<A[i];
    cout<<endl;
  }
  return 0;
}
